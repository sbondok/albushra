
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
    autoprefixer: {}, // Autoprefixer is typically included/handled by v4 plugin but keeping for safety unless conflicting
  },
};

export default config;
